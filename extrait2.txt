          </View>
        </View>

        <View style={styles.section}>
          <Text style={styles.sectionTitre}>{t.prochainesPeriodes}</Text>
          {prochainsEvs.map((ev) => {
            const parent = parents[ev.parentId];
            return (
              <View key={ev.id} style={styles.carteEv}>
                <View style={[styles.barreEv, { backgroundColor: parent.couleur }]} />
                <View style={styles.contenuEv}>
                  <Text style={styles.evParent}>{parent.nom}</Text>
                  <Text style={styles.evDate}>
                    {format(parseISO(ev.dateDebut), 'd MMM', { locale })}
                    {'  →  '}
                    {format(parseISO(ev.dateFin), 'd MMM yyyy', { locale })}
                  </Text>
                  <Text style={styles.evType}>{t.typesGarde[ev.type]}</Text>
                </View>
              </View>
            );
          })}
        </View>

        <View style={{ height: SPACING.xxxl }} />
      </ScrollView>

      <Modal visible={modalVisible} animationType="slide" transparent onRequestClose={() => setModalVisible(false)}>
        <View style={styles.modalOverlay}>
          <View style={styles.modalCard}>
            <ScrollView showsVerticalScrollIndicator={false}>
              <Text style={styles.modalTitle}>{t.modalTitre}</Text>

              <Text style={styles.fieldLabel}>{t.parent}</Text>
              <View style={styles.pillRow}>
                {(['A', 'B'] as ParentRole[]).map((role) => {
                  const active = formParent === role;
                  return (
                    <Pressable
                      key={role}
                      onPress={() => setFormParent(role)}
                      style={[styles.pill, active && { backgroundColor: parents[role].couleur, borderColor: parents[role].couleur }]}
                    >
                      <Text style={[styles.pillText, active && styles.pillTextActive]}>{parents[role].nom.split(' ')[0]}</Text>
                    </Pressable>
                  );
